<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.easyfieldregapps">

    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <receiver android:name=".Broadcaster"/>
        <activity android:name=".MailActivity"></activity>
        <activity android:name=".Vibrate" />
        <activity android:name=".PhonecallActivity" />
        <activity android:name=".Homeimplicitintent" />
        <activity android:name=".Alarm" />
        <activity android:name=".HomecountActivity" />
        <activity android:name=".HomebatteryActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>